#include <stdio.h>

int main () {
    char lixo [7] [30], uf3 [30], uf7 [30], uf9 [30];
    gets(lixo[0]);
    gets(lixo[1]);
    gets(uf3);
    gets(lixo[2]);
    gets(lixo[3]);
    gets(lixo[4]);
    gets(uf7);
    gets(lixo[5]);
    gets(uf9);
    gets(lixo[6]);

    printf ("%s\n", uf3);
    printf ("%s\n", uf7);
    printf ("%s\n", uf9);

    return 0;
}